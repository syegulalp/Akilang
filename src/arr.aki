# Demo of class definitions across function boundaries

uni {
    z:i32[8]
}

def fn1(x:i32[8]):i32[8] {
    x[0]=64
    return x
}

def fn():ptr i32[8]{
    # var y="Hello"
    # print (y)
    var x:i32[8]
    x[0]=32
    return c_ref(x)
}

def main(){
    # var cc:i32[8]
    # cc[0]=16
    # print (cc[0])
    
    var x=c_deref(fn())
    print (x[0])
    
    #z[0]=32
    #print(z[0])
    0
}
# Simple Win32 MessageBox call example

extern MessageBoxW(hwnd:i32, msg:ptr u8, caption:ptr u8, type: u8):i32

def main(){    
    MessageBoxW(
        0,
        utf_to_wide('Yo there! こんにちは世界!'),
        utf_to_wide('Greetings'),
        0B)
    0
    }
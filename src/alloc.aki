def main(){
    var x=c_obj_alloc({with var z:u64[4] z})
    x[0]=c_addr(x)
    x[1]=c_addr(x[0])
    x[2]=c_addr(x[1])
    print (x[0])
    print (x[1])
    print (x[2])

    if c_obj_free(x) then print("OK") else print ("No")

    0
}
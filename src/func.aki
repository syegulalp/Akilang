
def c_print(cstr string, *operators) {
    printf(c_data(cstr), *operator)
}

def main(){
    #printf(c_data('Hello world %i %s %i'), 32U, c_data('Yo'))
    c_print('Hello world %i %s', 32U, 'Yo')
    # pack and unpack
    # If we pass a stararg then we need to save that in the function params and then
    # just pass it along when we invoke it again
    # they're all just generic ptr i8 with no type information
}